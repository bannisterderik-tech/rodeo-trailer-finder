/* ============================================================
   Haulin' Hooves — submission webhook
   ============================================================
   Paste your Google Apps Script Web App URL between the quotes
   below to start collecting beta leads + feedback in your Google
   Sheet. Both the landing page and the in-app feedback survey POST
   here. Leave it blank to keep everything in the browser
   (localStorage) only — nothing is sent anywhere.

   Setup steps + the Apps Script to paste are in SETUP.md.
   ============================================================ */
window.HH_WEBHOOK = "";
